[build]

    publish = "dist"

    environment = { NODE_VERSION = "16.14.0" }

    command = "npm run test:coverage && bash <(curl -Ls https://coverage.codacy.com/get.sh) report -r coverage/lcov.info && npm run build"

[dev]

    publish = "dist"

    command = "npm run dev"
